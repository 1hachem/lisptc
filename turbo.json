{
  "$schema": "https://turbo.build/schema.json",
  "envMode": "loose",
  "boundaries": {
    "tags": {
      "foundation": {
        "dependencies": {
          "deny": [
            "platform",
            "language",
            "extension",
            "runtime",
            "agent",
            "frontend",
            "product"
          ]
        }
      },
      "language": {
        "dependencies": {
          "deny": ["extension", "runtime", "agent", "frontend", "product"]
        }
      },
      "extension": {
        "dependencies": {
          "deny": ["runtime", "agent", "frontend", "product"]
        }
      },
      "runtime": {
        "dependencies": {
          "deny": ["agent", "frontend", "product"]
        }
      },
      "ui": {
        "dependencies": {
          "deny": ["language", "extension", "runtime", "agent", "product"]
        }
      },
      "product": {
        "dependents": {
          "allow": []
        }
      }
    }
  },
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "build": {
      "inputs": ["$TURBO_DEFAULT$"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"],
      "env": ["NEXT_PUBLIC_*", "VITE_*"],
      "dependsOn": ["^build"]
    },
    "typecheck": {
      "dependsOn": ["^typecheck"]
    },
    "test": {
      "dependsOn": ["^typecheck"],
      "outputs": []
    },
    "test:watch": {
      "cache": false,
      "persistent": true
    },
    "test:evals": {
      "cache": false
    }
  }
}
